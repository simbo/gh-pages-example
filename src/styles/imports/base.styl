@require 'vars'

*,
*::before,
*::after
  box-sizing: inherit

html
  font-size: 16px

  @media screen and (min-width: 768px)
    font-size: 18px

body
  font-family: $fontFamilies.system
  background: #fafbfc
  text-rendering: optimizeLegibility
